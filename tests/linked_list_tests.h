#ifndef LINKED_LIST_TESTS_H
#define LINKED_LIST_TESTS_H

/* Runs the full LinkedList test suite.
 * Returns 0 if all tests passed, non-zero otherwise.
 * Prints per-suite pass/fail summary.
 */
int run_linked_list_tests(int argc, char** argv);

#endif /* LINKED_LIST_TESTS_H */
